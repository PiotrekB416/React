import { createRoot } from "react-dom/client";

const Change = () => {
    createRoot(document.getElementById('roll_root')).render(
        <a href="https://www.google.com" target="_blank">Google</a>
    )
}

export default Change;